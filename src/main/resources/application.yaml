spring:
  datasource:
    url: jdbc:mysql://localhost:3306/burgerShopDb?createDatabaseIfNotExist=TRUE
    username: "root"
    password: "123"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# Дополнительные настройки
#logging:
#  level:
#    org.hibernate.SQL: debug
#    org.hibernate.type: trace